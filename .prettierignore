node_modules
dist
build
.turbo
pnpm-lock.yaml
.playwright-mcp

# ajv standalone output — byte-compared against a fresh codegen run by
# packages/dfos-explorer/tests/schema-validator-drift.spec.ts, so reformatting it
# would report drift on every check
packages/dfos-explorer/src/lib/generated

# openapi.yaml → openapi.json codegen (scripts/generate-openapi-json.mjs), byte-
# compared against a fresh run by packages/dfos-web-relay/tests/openapi.spec.ts —
# same reason: reformatting it would report drift on every check
packages/dfos-web-relay/openapi.json

# packages/protocol-verify/vectors.json — generated from the protocol's fixed seeds and
# byte-compared against a fresh generation by
# packages/dfos-protocol/tests/protocol-reference.spec.ts, so reformatting it would
# report drift on every check
packages/protocol-verify/vectors.json
