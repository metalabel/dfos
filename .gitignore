node_modules
.pnpm-store
dist
build
.turbo
*.tsbuildinfo
coverage
*.log
.pnpm-debug.log*
.DS_Store
.env.local
.env
notes.local
target
.build
.swiftpm
.astro
.wrangler
*.local.md
dist/goreleaser

# conformance / parity harness build artifacts (scripts build into tempdirs,
# but a manual `go build` can leave these in-tree)
packages/dfos-web-relay-go/dfos-test
packages/dfos-web-relay-go/parity-serve
