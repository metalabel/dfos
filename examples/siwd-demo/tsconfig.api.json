{
  "//": "The serverless half. Separate from tsconfig.json because api/ runs in Node with no DOM: giving it the browser lib would let server code reach for `window` and still typecheck.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "skipLibCheck": true,
    "isolatedModules": true
  },
  "include": ["api"]
}
