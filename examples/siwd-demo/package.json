{
  "name": "siwd-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Sign In With DFOS — a minimal static relying party",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.api.json"
  },
  "dependencies": {
    "@metalabel/dfos-client": "^0.30.0",
    "@metalabel/dfos-protocol": "^0.30.0",
    "@metalabel/dfos-web-relay": "^0.30.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
