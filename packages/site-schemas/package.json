{
  "name": "@metalabel/site-schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "build": "wrangler pages functions build --outdir dist",
    "deploy": "wrangler pages deploy public",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@metalabel/dfos-protocol": "workspace:*",
    "hono": "^4.12.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
