---
import fs from 'node:fs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../deploy/QUICKSTART.md', 'utf-8');
---

<SpecPage
  title="Deploy"
  description="Running a DFOS relay — Docker Compose with Caddy auto-TLS, configuration, peering, and container images."
  path="/deploy"
  source={source}
/>
