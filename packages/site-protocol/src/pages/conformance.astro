---
import fs from 'node:fs';
import { specEntry } from '../content/specs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/CONFORMANCE.md', 'utf-8');
const entry = specEntry('/conformance');
---

<SpecPage
  title={entry.title}
  description={entry.metaDescription}
  path="/conformance"
  source={source}
/>
