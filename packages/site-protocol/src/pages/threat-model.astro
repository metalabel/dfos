---
import fs from 'node:fs';
import { specEntry } from '../content/specs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/THREAT-MODEL.md', 'utf-8');
const entry = specEntry('/threat-model');
---

<SpecPage
  title={entry.title}
  description={entry.metaDescription}
  path="/threat-model"
  source={source}
/>
