---
import fs from 'node:fs';
import { specEntry } from '../content/specs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/CREDENTIALS.md', 'utf-8');
const entry = specEntry('/credentials');
---

<SpecPage
  title={entry.title}
  description={entry.metaDescription}
  path="/credentials"
  source={source}
/>
