---
import fs from 'node:fs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/CREDENTIALS.md', 'utf-8');
---

<SpecPage
  title="Credentials"
  description="DFOS Credentials — UCAN-style authorization tokens for delegated content access, revocation, and standing authorization."
  path="/credentials"
  source={source}
/>
