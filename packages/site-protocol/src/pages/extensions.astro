---
import fs from 'node:fs';
import { specEntry } from '../content/specs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/EXTENSIONS.md', 'utf-8');
const entry = specEntry('/extensions');
---

<SpecPage
  title={entry.title}
  description={entry.metaDescription}
  path="/extensions"
  source={source}
/>
