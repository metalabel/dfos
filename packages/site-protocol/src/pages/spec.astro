---
import fs from 'node:fs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/PROTOCOL.md', 'utf-8');
---

<SpecPage
  title="Specification"
  description="Complete DFOS protocol specification — Ed25519 signed chain primitives, services, credentials, countersignatures, identity and content verification, with worked examples and test vectors."
  path="/spec"
  source={source}
/>
