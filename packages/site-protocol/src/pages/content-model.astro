---
import fs from 'node:fs';
import SpecPage from '../layouts/SpecPage.astro';

const source = fs.readFileSync('../../specs/CONTENT-MODEL.md', 'utf-8');
---

<SpecPage
  title="Content Model"
  description="DFOS Content Model — standard JSON Schema content types (post, profile) committed via content-addressed CIDs."
  path="/content-model"
  source={source}
/>
